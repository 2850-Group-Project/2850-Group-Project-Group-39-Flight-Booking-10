<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Create Account - SkyFlow</title>
  <style>
    :root{
      --bg0:#0b0d12;
      --bg1:#101420;
      --panel:#0f1320;
      --line:rgba(255,255,255,.10);
      --line2:rgba(255,255,255,.14);
      --muted:rgba(255,255,255,.70);
      --muted2:rgba(255,255,255,.55);
      --text:#ffffff;
      --accent:#3f5efb;
      --accent2:#2b7bff;
      --danger:#ff4d4d;
      --shadow:0 20px 70px rgba(0,0,0,.55);
      --radius:18px;
    }

    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
      color:var(--text);
      background: linear-gradient(180deg, var(--bg0), var(--bg1));
      min-height:100vh;
    }

    .topbar{
      height:64px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:0 22px;
      border-bottom:1px solid rgba(255,255,255,.08);
      background: rgba(10,12,18,.55);
      backdrop-filter: blur(12px);
      position:sticky;
      top:0;
      z-index:20;
    }

    .brand{
      display:flex;
      align-items:center;
      gap:10px;
      font-weight:800;
      letter-spacing:.2px;
      opacity:.95;
    }
    .brand-badge{
      width:34px;height:34px;
      border-radius:12px;
      display:grid;place-items:center;
      background:rgba(255,255,255,.08);
      border:1px solid rgba(255,255,255,.10);
      box-shadow: 0 12px 30px rgba(0,0,0,.25);
      font-size:16px;
    }

    .top-actions{
      display:flex;
      align-items:center;
      gap:12px;
      color: rgba(255,255,255,.70);
      font-size:13px;
    }
    .top-actions a{
      text-decoration:none;
      color: rgba(255,255,255,.85);
      border:1px solid rgba(255,255,255,.16);
      background: rgba(255,255,255,.06);
      padding:8px 12px;
      border-radius:999px;
      font-weight:700;
      transition:.18s ease;
    }
    .top-actions a:hover{
      background: rgba(255,255,255,.10);
      transform: translateY(-1px);
    }

    .shell{
      min-height: calc(100vh - 64px);
      display:grid;
      grid-template-columns: 1.05fr .95fr;
    }

    .hero{
      position:relative;
      overflow:hidden;
      padding:44px 46px;
      background:
        radial-gradient(1100px 800px at 20% 15%, rgba(120,160,255,.25), transparent 60%),
        radial-gradient(900px 600px at 65% 70%, rgba(56,189,248,.14), transparent 60%),
        linear-gradient(135deg, rgba(255,255,255,.06), rgba(255,255,255,0));
      border-right:1px solid rgba(255,255,255,.06);
    }

    .hero .tag{
      display:inline-flex;
      align-items:center;
      gap:8px;
      padding:7px 12px;
      border-radius:999px;
      background: rgba(255,255,255,.10);
      border:1px solid rgba(255,255,255,.12);
      font-size:12px;
      color: rgba(255,255,255,.90);
      width:fit-content;
    }

    .hero h1{
      margin:18px 0 12px;
      font-size:54px;
      line-height:1.02;
      letter-spacing:-1px;
      max-width:520px;
    }
    .hero p{
      margin:0;
      max-width:540px;
      color:var(--muted);
      font-size:15px;
      line-height:1.65;
    }

    .feature-list{
      margin-top:26px;
      display:grid;
      gap:16px;
      max-width:560px;
    }

    .feature{
      display:flex;
      gap:14px;
      padding:14px 14px;
      border-radius:18px;
      background: rgba(15,19,32,.55);
      border:1px solid rgba(255,255,255,.10);
      backdrop-filter: blur(10px);
    }
    .ficon{
      width:34px;height:34px;
      border-radius:14px;
      display:grid;place-items:center;
      background: rgba(255,255,255,.10);
      border:1px solid rgba(255,255,255,.10);
      flex:0 0 auto;
      font-size:15px;
    }
    .feature b{
      display:block;
      font-size:14px;
      margin-bottom:4px;
    }
    .feature small{
      display:block;
      color:var(--muted2);
      line-height:1.45;
      font-size:12.5px;
    }

    .panel{
      display:flex;
      justify-content:center;
      align-items:center;
      padding:34px 22px;
    }

    .card{
      width:100%;
      max-width:520px;
      border-radius:var(--radius);
      background: rgba(15,19,32,.92);
      border:1px solid rgba(255,255,255,.10);
      box-shadow: var(--shadow);
      padding:28px 28px 22px;
    }

    .card h2{
      margin:0 0 6px;
      font-size:28px;
      letter-spacing:-.4px;
    }
    .card p.sub{
      margin:0 0 18px;
      color:var(--muted);
      font-size:13.5px;
      line-height:1.55;
    }

    .error{
      margin:0 0 14px;
      padding:12px 12px;
      border-radius:14px;
      background: rgba(255,77,77,.12);
      border:1px solid rgba(255,77,77,.25);
      color: #ffd0d0;
      font-size:13px;
      line-height:1.35;
    }

    .row2{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap:12px;
      margin-bottom:14px;
    }

    .btn{
      border:1px solid rgba(255,255,255,.14);
      background: rgba(255,255,255,.06);
      color: rgba(255,255,255,.92);
      padding:12px 12px;
      border-radius:14px;
      font-weight:700;
      cursor:pointer;
      display:flex;
      justify-content:center;
      align-items:center;
      gap:10px;
      transition: .18s ease;
      user-select:none;
    }
    .btn:hover{ background: rgba(255,255,255,.10); transform: translateY(-1px); }
    .btn:active{ transform: translateY(0px); }

    .divider{
      margin:18px 0 16px;
      display:flex;
      align-items:center;
      gap:12px;
      color: rgba(255,255,255,.55);
      font-size:12px;
      letter-spacing:.4px;
    }
    .divider::before, .divider::after{
      content:"";
      flex:1;
      height:1px;
      background: rgba(255,255,255,.10);
    }

    label{
      display:block;
      margin:10px 0 8px;
      color: rgba(255,255,255,.78);
      font-size:12.8px;
      font-weight:700;
    }

    .grid{
      display:grid;
      grid-template-columns: 1fr 1fr;
      gap:12px;
    }

    .input{
      width:100%;
      padding:13px 14px;
      border-radius:14px;
      border:1px solid rgba(255,255,255,.14);
      background: rgba(10,12,18,.55);
      color: white;
      outline:none;
      font-size:14px;
      transition:.18s ease;
    }
    .input:focus{
      border-color: rgba(63,94,251,.65);
      box-shadow: 0 0 0 4px rgba(63,94,251,.18);
    }

    .strength{
      margin-top:10px;
      display:flex;
      align-items:center;
      gap:8px;
      color: rgba(255,255,255,.70);
      font-size:12px;
    }
    .dot{
      width:10px;height:10px;border-radius:999px;
      background: rgba(34,197,94,.95);
      box-shadow: 0 0 0 4px rgba(34,197,94,.18);
    }

    .checks{
      margin-top:14px;
      display:grid;
      gap:10px;
      color: rgba(255,255,255,.70);
      font-size:12.6px;
      line-height:1.4;
    }

    .checks label{
      margin:0;
      display:flex;
      gap:10px;
      align-items:flex-start;
      font-weight:650;
      color: rgba(255,255,255,.74);
    }
    .checks input{
      margin-top:3px;
      transform: scale(1.05);
      accent-color: var(--accent);
    }
    .checks a{
      color: rgba(125,211,252,.95);
      text-decoration:none;
      font-weight:800;
    }
    .checks a:hover{ text-decoration:underline; }

    .primary{
      width:100%;
      margin-top:16px;
      padding:13px 14px;
      border:none;
      border-radius:14px;
      background: linear-gradient(90deg, var(--accent), var(--accent2));
      color:white;
      font-weight:900;
      font-size:14px;
      cursor:pointer;
      transition:.18s ease;
    }
    .primary:hover{ filter:brightness(1.06); transform: translateY(-1px); }
    .primary:active{ transform: translateY(0px); }

    .secure{
      margin-top:14px;
      padding-top:14px;
      border-top:1px solid rgba(255,255,255,.08);
      display:flex;
      align-items:center;
      gap:10px;
      color: rgba(255,255,255,.60);
      font-size:12px;
    }
    .lock{
      width:28px;height:28px;border-radius:12px;
      display:grid;place-items:center;
      background: rgba(255,255,255,.08);
      border:1px solid rgba(255,255,255,.10);
    }

    .bottombar{
      height:58px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:0 22px;
      border-top:1px solid rgba(255,255,255,.08);
      background: rgba(10,12,18,.55);
      color: rgba(255,255,255,.55);
      font-size:12px;
    }
    .bottombar a{
      color: rgba(255,255,255,.55);
      text-decoration:none;
    }
    .bottombar a:hover{ text-decoration:underline; }
    .links{
      display:flex;
      gap:14px;
      flex-wrap:wrap;
    }

    @media (max-width: 980px){
      .shell{ grid-template-columns: 1fr; }
      .hero{ display:none; }
      .panel{ padding:22px 14px; }
      .card{ padding:24px 18px 18px; }
      .grid{ grid-template-columns: 1fr; }
    }
  </style>
</head>

<body>
  <header class="topbar">
    <div class="brand">
      <div class="brand-badge">✈</div>
      <div>SkyFlow</div>
    </div>
    <div class="top-actions">
      <span>Already have an account?</span>
      <a href="/login">Log In</a >
    </div>
  </header>

  <div class="shell">
    <section class="hero">
      <div class="tag">SKYFLOW LOYALTY</div>

      <h1>Your global adventure starts here.</h1>

      <p>
        Join millions of travelers exploring the world with curated flights, exclusive lounge access,
        and premium rewards.
      </p >

      <div class="feature-list">
        <div class="feature">
          <div class="ficon">⭐</div>
          <div>
            <b>Earn Miles Instantly</b>
            <small>Get bonus miles for signing up today. Redeem on any flight.</small>
          </div>
        </div>

        <div class="feature">
          <div class="ficon">⚡</div>
          <div>
            <b>Priority Booking</b>
            <small>Early access to seasonal sales and flash deals before everyone else.</small>
          </div>
        </div>

        <div class="feature">
          <div class="ficon">🎁</div>
          <div>
            <b>Exclusive Perks</b>
            <small>Member-only upgrades, extra baggage, and complimentary in-flight Wi-Fi.</small>
          </div>
        </div>
      </div>
    </section>

    <main class="panel">
      <div class="card">
        <h2>Create your SkyFlow account</h2>
        <p class="sub">Sign up to unlock exclusive flight deals and manage your trips effortlessly.</p >

        {% if error %}
          <div class="error">{{ error }}</div>
        {% endif %}

        <div class="row2">
          <button class="btn" type="button" onclick="return false;">Google</button>
          <button class="btn" type="button" onclick="return false;">Apple</button>
        </div>

        <div class="divider">OR CONTINUE WITH EMAIL</div>

        <form action="/register" method="post">
          <div class="grid">
            <div>
              <label for="firstName">First Name</label>
              <input class="input" id="firstName" name="firstName" type="text" placeholder="e.g., Jonathan" />
            </div>
            <div>
              <label for="lastName">Last Name</label>
              <input class="input" id="lastName" name="lastName" type="text" placeholder="e.g., Wick" />
            </div>
          </div>

          <label for="email">Email Address</label>
          <input class="input" id="email" type="email" name="email" placeholder="name@example.com" required />

          <label for="password">Create Password</label>
          <input class="input" id="password" type="password" name="password" placeholder="••••••••" required />

          <label for="confirmPassword">Confirm Password</label>
          <input class="input" id="confirmPassword" type="password" name="confirmPassword" placeholder="••••••••" required />

          <div class="strength">
            <span class="dot"></span>
            <span>STRONG PASSWORD</span>
          </div>

          <div class="checks">
            <label>
              <input type="checkbox" checked />
              <span>Join SkyFlow Loyalty Program<br/><span style="color:rgba(255,255,255,.55);font-weight:600;">Enroll for free to start earning miles on every flight and unlock exclusive lounge access.</span></span>
            </label>

            <label>
              <input type="checkbox" required />
              <span>I agree to the <a href="#" onclick="return false;">Terms of Service</a > and <a href="#" onclick="return false;">Privacy Policy</a >.</span>
            </label>
          </div>

          <button class="primary" type="submit">Create Account →</button>
        </form>

        <div class="secure">
          <div class="lock">🔒</div>
          <div>Secure 256-bit SSL Encrypted Connection</div>
        </div>
      </div>
    </main>
  </div>

  <footer class="bottombar">
    <div class="links">
      <a href="#" onclick="return false;">Privacy Policy</a >
      <a href="#" onclick="return false;">Terms of Service</a >
      <a href="#" onclick="return false;">Cookie Settings</a >
      <a href="#" onclick="return false;">Contact Support</a >
    </div>
    <div>© 2026 SkyBook. All rights reserved</div>
  </footer>
</body>
</html>
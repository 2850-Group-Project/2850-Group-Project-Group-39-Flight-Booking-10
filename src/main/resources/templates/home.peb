<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/static/css/Home.css">
  <title>SkyFlow - Find Your Flight</title>
</head>
<body>
  <!-- NAV -->
  <nav>
    <a href="/" class="brand">
      <div class="brand-badge">✈️</div>
      <span class="brand-title">SkyFlow</span>
    </a>
    <div class="nav-right">
      <a href="#" class="btn-ghost" data-large-font-toggle>Large Font</a>
      <a href="/profile/bookings" class="btn-ghost">My Bookings</a>
      <a href="/profile" class="btn-primary">My Account</a>
    </div>
  </nav>

  <!-- HERO -->
  <main>
    <section class="hero">
      <h1>Where are we flying to,<br /><span>{{ userSession.firstName }}?</span></h1>

      <!-- SEARCH CARD -->
      <div class="search-card">

        <!-- TRIP TYPES -->
        <div class="trip-tabs">
          <button class="trip-tab active" onclick="setTripType('return', this)" aria-label="Select return trip">⇄ Return</button>
          <button class="trip-tab" onclick="setTripType('oneway', this)" aria-label="Select one way trip">→ One way</button>
        </div>

        <!-- SEARCH FORM -->
        <form action="/flights/search" method="GET" id="searchForm">
          <input type="hidden" name="trip_type" id="tripType" value="return" />

          <div class="search-row">

            <!-- ORIGIN -->
            <div class="field">
              <label>From</label>
              <div class="field-inner autocomplete-wrapper">
                <input
                  type="text"
                  id="origin-input"
                  placeholder="City or airport"
                  autocomplete="off"
                  required
                />
                <input type="hidden" name="origin" id="origin-value" />
                <ul class="autocomplete-dropdown" id="origin-dropdown"></ul>
              </div>
            </div>

            <!-- SWAP -->
            <div class="swap-btn">
              <button type="button" onclick="swapAirports()" title="Swap airports" aria-label="Swap origin and destination airports">⇄</button>
            </div>

            <!-- DESTINATION -->
            <div class="field">
              <label>To</label>
              <div class="field-inner autocomplete-wrapper">
                <input
                  type="text"
                  id="destination-input"
                  placeholder="City or airport"
                  autocomplete="off"
                  required
                />
                <input type="hidden" name="destination" id="destination-value" />
                <ul class="autocomplete-dropdown" id="destination-dropdown"></ul>
              </div>
            </div>

            <!-- DEPARTURE DATE -->
            <div class="field">
              <label>Departure</label>
              <div class="field-inner">
                <input type="date" name="departure_date" required />
              </div>
            </div>

            <!-- RETURN DATE -->
            <div class="field" id="returnField">
              <label>Return</label>
              <div class="field-inner">
                <input type="date" name="return_date" id="returnDate" required />
              </div>
            </div>

            <!-- PASSENGERS -->
            <div class="field pax-wrapper">
              <label>Passengers</label>
              <div class="pax-display" onclick="togglePax()">
                <span id="paxLabel">1 Adult</span>
                <span class="caret">▼</span>
              </div>
              <div class="pax-dropdown" id="paxDropdown">
                <div class="pax-row">
                  <div>
                    <div class="pax-label">Adults</div>
                    <div class="pax-sub">16+ years</div>
                  </div>
                  <div class="pax-ctrl">
                    <button type="button" onclick="changePax('adults', -1)" aria-label="Decrease adults">-</button>
                    <span class="count" id="adultsCount">1</span>
                    <button type="button" onclick="changePax('adults', 1)" aria-label="Increase adults">+</button>
                  </div>
                </div>
                <div class="pax-row">
                  <div>
                    <div class="pax-label">Children</div>
                    <div class="pax-sub">2-15 years</div>
                  </div>
                  <div class="pax-ctrl">
                    <button type="button" onclick="changePax('children', -1)" aria-label="Decrease children">-</button>
                    <span class="count" id="childrenCount">0</span>
                    <button type="button" onclick="changePax('children', 1)" aria-label="Increase children">+</button>
                  </div>
                </div>
                <div class="pax-row">
                  <div>
                    <div class="pax-label">Infants</div>
                    <div class="pax-sub">Under 2 years</div>
                  </div>
                  <div class="pax-ctrl">
                    <button type="button" onclick="changePax('infants', -1)" aria-label="Decrease infants">-</button>
                    <span class="count" id="infantsCount">0</span>
                    <button type="button" onclick="changePax('infants', 1)" aria-label="Increase infants">+</button>
                  </div>
                </div>
              </div>
              
              <!-- HIDDEN: INPUTS SUBMITTED WITH FORM -->
              <input type="hidden" name="adults" id="adultsInput" value="1" />
              <input type="hidden" name="children" id="childrenInput" value="0" />
              <input type="hidden" name="infants" id="infantsInput" value="0" />
            </div>
          </div>

          <!-- SEARCH -->
          <div>
            <button type="submit" class="btn-search" aria-label="Search flights">Search flights</button>
          </div>
        </form>
      </div>
    </section>
  </main>

  <!-- FOOTER -->
  <footer>
    <span>© 2026 SkyBook. All rights reserved</span>
    <div class="footer-links">
      <a href="/privacy">Privacy</a>
      <a href="/terms">Terms</a>
      <a href="/help">Help</a>
      <a href="/complaints">Complaints</a>
    </div>
  </footer>

  <script src="/static/javascript/Home.js"></script>
  <script src="/static/javascript/Large_Font.js"></script>
</body>
</html>
